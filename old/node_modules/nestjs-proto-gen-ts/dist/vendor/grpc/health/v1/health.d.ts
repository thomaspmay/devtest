import { Observable } from 'rxjs';
import { Metadata } from 'grpc';
export declare namespace grpc {
    namespace health {
        namespace v1 {
            interface HealthCheckRequest {
                service?: string;
            }
            interface HealthCheckResponse {
                status?: grpchealthv1.ServingStatus;
            }
            interface Health {
                check(data: HealthCheckRequest, metadata?: Metadata): Observable<HealthCheckResponse>;
                watch(data: HealthCheckRequest, metadata?: Metadata): Observable<HealthCheckResponse>;
            }
        }
    }
}
export declare namespace grpchealthv1 {
    enum ServingStatus {
        UNKNOWN = 0,
        SERVING = 1,
        NOT_SERVING = 2,
        SERVICE_UNKNOWN = 3
    }
}
